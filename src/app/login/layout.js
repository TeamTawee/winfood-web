export const metadata = {
  title: "Staff Login | Winfood Portal", // 🟢 เปลี่ยนชื่อตรงนี้
};

export default function LoginLayout({ children }) {
  return children;
}